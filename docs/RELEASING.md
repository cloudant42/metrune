# Release procedure

Metrune is a production beta. The server and client are released
independently, while the client artifacts for Linux, Windows, and macOS are
always published together. The versioning and compatibility rules are
canonical in [VERSIONING.md](VERSIONING.md).

## Prepare

1. Update the version for the release line being shipped and `CHANGELOG.md`.
   The first server release and first client release are both `0.1.0`.
2. Run `make release-check`, which includes `make check`, SQL-backed
   integration tests, browser flows, and the restore drill.
3. Review migration ordering and rollback limits, including migration 017's
   client-version telemetry columns.
4. Verify invitation and reset delivery against the production SMTP provider.
5. Exercise install, enrollment, upload, and analytics on a clean Linux host.
6. Create and push an annotated `server-vX.Y.Z` or `client-vX.Y.Z` tag from
   the reviewed commit. Use a separate tag when the other line is released;
   do not reuse a server tag for client artifacts.

The tag is not a versioning mechanism by itself: the server workspace version,
the client package version, the binary `--version`, and request headers must
equal the release semver (with only the documented `v` display prefix). The
release job validates the namespaced tag and package version before publishing.
It must also fail before publishing when the signing key, pinned public key,
or version assertion is missing.

## Client artifacts

The `client-vX.Y.Z` workflow builds and runs `--version` and `--help` on Linux
x86_64, Windows x86_64, Intel macOS, and Apple Silicon. It publishes checksums,
a support-tier manifest, build-provenance attestations, and
`client-manifest.json` — the machine-readable manifest `metrune update` and
every mirroring server read. All four OS artifacts use that one client tag and
one client version.

That manifest is generated by the released Linux binary itself, so its format
cannot drift from the client that parses it, and it is signed with
`METRUNE_RELEASE_SIGNING_KEY`. Set the `MINIMUM_CLIENT_VERSION` repository
variable to publish the requested version floor. Operators enforce the same
floor with `METRUNE_MINIMUM_CLIENT_VERSION` only after installation telemetry
shows that the rollout is ready. Distribution, mirroring, compatibility
rejections, and key rotation are described in
[CLIENT_DISTRIBUTION.md](CLIENT_DISTRIBUTION.md).

Linux x86_64 is supported in the beta. Windows and macOS artifacts are
experimental until platform-specific enrollment, credential-store, and
watch-mode tests are release gates.

## Server images

The `server-vX.Y.Z` workflow builds the API and web images from the reviewed
server release commit, generates image SBOMs and provenance, scans for
high/critical findings, and publishes `SERVER_IMAGE_MANIFEST.txt`. No moving
`latest` tag is published. A server release does not publish a client version;
production deployments mount a separately selected, signed `client-vX.Y.Z`
manifest and its artifacts.

Put its immutable digests in the private production environment:

```dotenv
METRUNE_API_IMAGE=ghcr.io/<owner>/<repo>-api@sha256:<digest>
METRUNE_WEB_IMAGE=ghcr.io/<owner>/<repo>-web@sha256:<digest>
```

Verify an attestation from a clean machine:

```bash
gh attestation verify \
  oci://ghcr.io/<owner>/<repo>-api@sha256:<digest> \
  --repo <owner>/<repo>
```

Back up PostgreSQL, ClickHouse, and the vault key before rollout. Start the new
API first, verify `/v1/readyz` and `/v1/server/info`, then recreate the web
service. Keep previous images and backups until the rollback window closes.

## Web safety gate

- Signed-in pages never replace an API failure with fixture organization data.
- Demo fixtures require `METRUNE_ENABLE_DEMO_DATA=1`, are disabled in
  production, and are never used when a browser session is present.
- Missing roles cannot open administration/pricing or session-drilldown UI;
  backend role checks remain authoritative.
- `/api/export` preserves filters, returns an error instead of an empty success
  file when live data is unavailable, marks CSV `no-store`, and neutralizes
  spreadsheet formula prefixes.
- Login, SSO, workspace-selection, and device continuations accept only
  same-origin relative paths.

## Fixture and external-gate tracking

Every verification claim must identify the exact fixture or external boundary:

- Adapter fixtures live under `tests/fixtures` or the documented generated
  fixture directory and must be named in `docs/FUNCTIONAL_VERIFICATION.md`.
- `Verified` means the fixture or service was executed in this checkout;
  `Covered` means a deterministic substitute or lower-level contract;
  `Not executed` means an explicit operator/provider/platform gate remains.
- Do not mark real SMTP, paid provider traffic, canonical signed-release
  self-install, customer IdP federation, Windows/macOS builds, or long-running
  restore/soak behavior as verified without an attached run artifact.

## Compatibility policy before 1.0

- A server release accepts the current and immediately previous upload schema.
- A client or schema floor is announced in the signed manifest and changelog
  for at least one release before support is removed.
- The compatibility release ships the typed HTTP 426 response and client update
  path before an operator can enforce the new floor.
- `MINIMUM_CLIENT_VERSION` in the manifest and the documented deployment value
  for `METRUNE_MINIMUM_CLIENT_VERSION` use complete semantic versions and agree
  when the rollout closes.
- Database migrations remain compatible with the previous server image for the
  stated rollback window, or the release notes explicitly require backup
  restoration and prohibit image-only rollback.
- A floor raise and removal of the previous schema never happen in the same
  release.

## Release gate

- `make check` and `git diff --check` pass.
- `make release-check` completes without skipped integration tests or leftover
  test containers.
- `scripts/restore-drill.sh` passes from a clean project.
- Both images pass the configured vulnerability scan.
- Changelog, support matrix, migration notes, and image digests agree.
- `/v1/server/info` reports the tagged server version, intended schema window,
  and intended client floor; the selected client manifest reports its own
  `client-vX.Y.Z` release.
- A below-floor client receives structured `client_unsupported` HTTP 426,
  exits watch/upload without retrying, and retains its local outbox.
- Organization and personal installation views show the version reported by a
  current client.
- SMTP delivery and a clean-host Linux client flow have been exercised.
- `client-manifest.json` is attached to the release, is signed, and
  `metrune update --check` against a staging server reports the new version.
